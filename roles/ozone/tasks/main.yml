---
- name: "Placeholder: Download the Ozone HIS distribution artifact from GCS"
  debug:
    msg: "This task will download the ozone-distro.zip from Google Cloud Storage."

- name: "Placeholder: Unzip the Ozone HIS distribution"
  debug:
    msg: "This task will unzip the artifact to /home/hello/ozone-dist."

- name: Deploy Ozone HIS services using shell
  become: true
  shell:
    cmd: "docker compose up -d"
    chdir: "/home/hello/ozone-dist"
  ignore_errors: true
